define(function() {

    /**
     * @desc 界面的构造函数;
     */
    var Page = P(EventBase, function( page ) {

        /**
         * @desc 界面的初始化;
         */
        page.init = function () {

        };

        /**
         * @desc 删除当前界面;
         */
        page.destory = function () {

        };

        /**
         * @desc 界面的创建;
         */
        page.create = function () {

        };

    });

    return Page;
})